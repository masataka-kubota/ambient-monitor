/**
 * Start angle in degrees for C-shape gauge / arc SVG paths.
 * Used with `describeArc` and gauge rendering.
 */
export const START_ANGLE = -135;

/**
 * End angle in degrees for C-shape gauge / arc SVG paths.
 * Paired with `START_ANGLE` for a 270° open arc.
 */
export const END_ANGLE = 135;
