/**
 * Placeholder cloud / API device id until dynamic device selection is implemented.
 *
 * Used as the default for `selectedDeviceIdAtom` and measurement queries.
 */
export const DEFAULT_DEVICE_ID = 'device-001';
